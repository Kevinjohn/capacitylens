# CVE-2026-47219 affects find-my-way's HTTP/2 request-routing path. CapacityLens creates an
# HTTP/1 Fastify server and packaged nginx explicitly proxies HTTP/1.1, so that path is unreachable.
CVE-2026-47219
