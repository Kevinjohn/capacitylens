import{_ as s,o as a,c as t,a3 as i}from"./chunks/framework.CchDE9cb.js";const k=JSON.parse('{"title":"Backups and restore","description":"What CapacityLens backs up automatically, and the exact steps to restore a snapshot when you need one.","frontmatter":{"title":"Backups and restore","description":"What CapacityLens backs up automatically, and the exact steps to restore a snapshot when you need one."},"headers":[],"relativePath":"self-hosting/backups-and-restore.md","filePath":"self-hosting/backups-and-restore.md","lastUpdated":null}'),n={name:"self-hosting/backups-and-restore.md"};function o(l,e,r,p,h,c){return a(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="backups-and-restore" tabindex="-1">Backups and restore <a class="header-anchor" href="#backups-and-restore" aria-label="Permalink to &quot;Backups and restore&quot;">​</a></h1><p>CapacityLens can take scheduled backups of its SQLite database automatically. This page covers what gets backed up, how the automatic snapshots work, and — as a complete, standalone procedure — how to restore one. If you&#39;re here because something has gone wrong, skip straight to <a href="#restore-a-snapshot">Restore a snapshot</a>.</p><h2 id="what-to-back-up" tabindex="-1">What to back up <a class="header-anchor" href="#what-to-back-up" aria-label="Permalink to &quot;What to back up&quot;">​</a></h2><ul><li><strong>The SQLite database file</strong> — the single source of truth for every company, person, project and allocation.</li><li><strong>Its <code>-wal</code> and <code>-shm</code> sidecar files</strong>, if present, whenever you copy it by hand — see the warning below.</li><li><strong>The audit log</strong> (JSONL). The database and the audit log share a single durability boundary: SQLite retains pending audit events until they&#39;re durably written to disk, so a complete recovery bundle includes both.</li></ul><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Never <code>cp</code> a live database file as a backup. SQLite keeps recent writes in the <code>-wal</code> file, so a plain file copy of <code>capacitylens.db</code> alone can be missing data or be internally inconsistent. Use the scheduled snapshot mechanism below, or stop the API first if you must copy files by hand.</p></div><h2 id="automatic-snapshots" tabindex="-1">Automatic snapshots <a class="header-anchor" href="#automatic-snapshots" aria-label="Permalink to &quot;Automatic snapshots&quot;">​</a></h2><p>Set <code>CAPACITYLENS_BACKUP_DIR</code> to a directory and CapacityLens takes an online SQLite backup at boot and on a configurable interval (<code>CAPACITYLENS_BACKUP_INTERVAL_MIN</code>, default 60 minutes) — this uses SQLite&#39;s own backup API, not a file copy, so it&#39;s safe to run against a live database. Docker Compose turns this on by default, snapshotting into the <code>capacitylens-backups</code> volume; set <code>CAPACITYLENS_BACKUP_DIR=</code> (explicitly empty) to turn it off.</p><p>Every snapshot is verified — an integrity check, a foreign-key check and a schema-version check — before it&#39;s given its final name or counted for retention. A failed check is logged as <code>backup FAILED</code>, the unpublished attempt is discarded, and every older restore point is left untouched. <code>CAPACITYLENS_BACKUP_KEEP</code> (default 48, maximum 10,000) controls how many snapshots are retained before the oldest are pruned.</p><p>New snapshots are named with a UTC-sortable timestamp: <code>capacitylens-utc-YYYYMMDD-HHMMSS-sss.db</code>. Older <code>capacitylens-YYYYMMDD-HHMMSS-sss.db</code> local-time snapshots (from earlier releases) remain valid restore inputs.</p><p>The on-host snapshot directory protects against many application and operator mistakes, but not loss of the whole host. For real disaster recovery, also copy snapshots off-host — to a separate account, region or provider, with tools like restic, rclone or rsync — encrypt that destination, and monitor snapshot freshness. Deep health reports <code>backup.status</code> and <code>backup.lastSuccessAt</code>; see <a href="/capacitylens/self-hosting/monitoring">Monitoring and health checks</a>.</p><h2 id="schedule-a-restore-drill" tabindex="-1">Schedule a restore drill <a class="header-anchor" href="#schedule-a-restore-drill" aria-label="Permalink to &quot;Schedule a restore drill&quot;">​</a></h2><p>Rehearse this before launch, and again after any material storage change — new host, new volume driver, new backup destination. Don&#39;t let the first real restore be the first time anyone has run it.</p><h2 id="restore-a-snapshot" tabindex="-1">Restore a snapshot <a class="header-anchor" href="#restore-a-snapshot" aria-label="Permalink to &quot;Restore a snapshot&quot;">​</a></h2><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Restoring replaces the live database with an older snapshot. Anything written after that snapshot&#39;s timestamp is lost from the live database (it&#39;s still recoverable from an even older snapshot only if that data existed there too). Preserve the current database before you start — steps 1-2 below do this for you.</p></div><h3 id="general-procedure" tabindex="-1">General procedure <a class="header-anchor" href="#general-procedure" aria-label="Permalink to &quot;General procedure&quot;">​</a></h3><ol><li>Stop the API cleanly and wait for it to exit.</li><li>Copy the live database and its <code>-wal</code>/<code>-shm</code> sidecars somewhere safe, in case you need to roll back the restore itself.</li><li>Copy a selected dated <code>capacitylens-utc-YYYYMMDD-HHMMSS-sss.db</code> scheduled snapshot to the configured database path. Don&#39;t select a <code>capacitylens-pre-migration-*</code> file here — those belong to the <a href="/capacitylens/self-hosting/upgrades">upgrade rollback procedure</a> instead.</li><li>Remove any stale <code>-wal</code> and <code>-shm</code> sidecars next to the restored file.</li><li>Start the API and check deep health.</li><li>Verify sign-in, the account list, recent expected data and one safe write.</li><li>Record the snapshot time, the result and how long the recovery took.</li></ol><h3 id="docker-compose-named-volume-procedure" tabindex="-1">Docker Compose named-volume procedure <a class="header-anchor" href="#docker-compose-named-volume-procedure" aria-label="Permalink to &quot;Docker Compose named-volume procedure&quot;">​</a></h3><p>The packaged Compose deployment stores <code>/data</code> and <code>/backups</code> in named Docker volumes, so host-path <code>cp</code> commands can&#39;t reach them. Run everything below from the checkout that has the active <code>.env</code> and <code>docker-compose.yml</code>. The commands use the API image&#39;s normal unprivileged <code>node</code> user, so the restored files keep the ownership the API expects.</p><p>Before you start, confirm <code>docker compose config</code> renders the physical volume prefix this installation actually uses. New deployments use <code>capacitylens_</code>. A deployment created before the project name was pinned may carry a different historical prefix through <code>COMPOSE_PROJECT_NAME</code> in <code>.env</code> — don&#39;t remove or change that override.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>If an upgrade unexpectedly presents an empty instance, do not create a company or remove volumes. Stop the new stack, run <code>docker volume ls</code>, restore the previous project prefix in <code>.env</code>, confirm the rendered <code>*_capacitylens-db</code>, <code>*_capacitylens-backups</code> and <code>*_capacitylens-internal-tls</code> names, then start the stack and verify the expected account list. Creating a company against the wrong volume set is not recoverable by restoring later.</p></div><ol><li><p>Stop the API, list the available snapshots, and preserve the cleanly stopped live database inside the backups volume. This fails before touching <code>/data</code> if no snapshot is present:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stop</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> api</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --rm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --no-deps</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --entrypoint</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> api</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -eu</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  ls -l /backups/capacitylens-*.db</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  rollback_dir=&quot;/backups/manual-restore-$(date -u +%Y%m%dT%H%M%SZ)&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  umask 077</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  mkdir -m 700 &quot;$rollback_dir&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  for file in /data/capacitylens.db /data/capacitylens.db-wal /data/capacitylens.db-shm; do</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    test ! -e &quot;$file&quot; || cp -p &quot;$file&quot; &quot;$rollback_dir/&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  done</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  printf &quot;Preserved stopped database files in %s\\n&quot; &quot;$rollback_dir&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;</span></span></code></pre></div></li><li><p>Choose the exact filename from that listing, then copy it into place, verify its mode and owner, atomically replace the live database, and remove sidecars. The basename check keeps the value confined to <code>/backups</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> RESTORE_SNAPSHOT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">capacitylens-utc-YYYYMMDD-HHMMSS-sss.db</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --rm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --no-deps</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> RESTORE_SNAPSHOT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --entrypoint</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> api</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -eu</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  case &quot;$RESTORE_SNAPSHOT&quot; in</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    capacitylens-*.db) ;;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    *) echo &quot;RESTORE_SNAPSHOT must be a capacitylens-*.db basename&quot; &gt;&amp;2; exit 1 ;;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  esac</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  case &quot;$RESTORE_SNAPSHOT&quot; in</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    */*) echo &quot;RESTORE_SNAPSHOT must not contain a path&quot; &gt;&amp;2; exit 1 ;;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  esac</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  source=&quot;/backups/$RESTORE_SNAPSHOT&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  target=/data/capacitylens.db</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  temporary=&quot;$target.restore&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  test -f &quot;$source&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  umask 077</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  cp &quot;$source&quot; &quot;$temporary&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  chmod 600 &quot;$temporary&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  test &quot;$(stat -c %a &quot;$temporary&quot;)&quot; = 600</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  test &quot;$(stat -c %u &quot;$temporary&quot;)&quot; = &quot;$(id -u)&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  mv &quot;$temporary&quot; &quot;$target&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  rm -f &quot;$target-wal&quot; &quot;$target-shm&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">unset</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> RESTORE_SNAPSHOT</span></span></code></pre></div></li><li><p>Start the API back up and check it:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> api</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ps</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -fsS</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://capacity.example.com/api/health</span></span></code></pre></div></li><li><p>Complete the sign-in, account, recent-data and safe-write checks from the general procedure above before removing the preserved <code>manual-restore-*</code> directory from step 1.</p></li></ol><p>If <code>CAPACITYLENS_DB</code> or <code>CAPACITYLENS_BACKUP_DIR</code> was deliberately changed from the packaged paths, adapt and rehearse this procedure for those mounts before you actually need it.</p><p><code>server/src/restore.drill.test.ts</code> continuously exercises the backup, corruption and restore path in CI, but that&#39;s not a substitute for an operator running this drill with real storage and credentials.</p><h2 id="what-s-next" tabindex="-1">What&#39;s next <a class="header-anchor" href="#what-s-next" aria-label="Permalink to &quot;What&#39;s next&quot;">​</a></h2><ul><li><a href="/capacitylens/self-hosting/upgrades">Upgrades</a> covers the separate pre-migration snapshot and application-rollback procedure for a schema-bearing release.</li><li><a href="/capacitylens/self-hosting/monitoring">Monitoring and health checks</a> for watching backup freshness day to day.</li></ul>`,25)])])}const u=s(n,[["render",o]]);export{k as __pageData,u as default};
