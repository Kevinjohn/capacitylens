/** Security-sensitive session windows shared by identity adapters and application policy. */
export const ACCOUNT_SESSION_ABSOLUTE_TTL_SECONDS = 12 * 60 * 60
export const ACCOUNT_SESSION_FRESH_AGE_SECONDS = 15 * 60
